apiVersion: v1
kind: Service
metadata:
  name: {{ include "zentinel-waf.fullname" . }}
  labels:
    {{- include "zentinel-waf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: health
      port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "zentinel-waf.selectorLabels" . | nindent 4 }}
